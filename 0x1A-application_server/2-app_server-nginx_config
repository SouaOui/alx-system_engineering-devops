server {
    listen 80 default_server;
    listen [::]:80 default_server;

    # Server name can be your IP address or a domain if you have one
    server_name 52.204.117.41;

    index index.html index.htm;

    location / {
        # Uncomment the try_files directive if you want to handle static files
        # try_files $uri $uri/ =404;

        include proxy_params;
        proxy_pass http://127.0.0.1:5000/airbnb-onepage/;
    }

    # Additional configuration for handling PHP scripts if needed
    # location ~ \.php$ {
    #     include snippets/fastcgi-php.conf;
    #     fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
    # }

    # Additional security measures (commented out, uncomment if needed)
    # location ~ /\.ht {
    #     deny all;
    # }
}
